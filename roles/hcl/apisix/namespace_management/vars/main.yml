__sudo_user:                      "{{ kubectl_user | default( ansible_facts['env']['SUDO_USER'] ) | default('lcuser') }}"
__apisix_namespace:               "{{ apisix_namespace | default('connections') }}"
