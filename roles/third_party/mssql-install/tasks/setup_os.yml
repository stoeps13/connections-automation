- name:                  Install pre-requisites Yum
  ansible.builtin.package:
    name:                ['unzip', 'dos2unix']
    state:               present
  when:
    - ansible_os_family == "RedHat"
