---
__calico_version:               "{{ calico_version | default('3.32.1') }}"
__cilium_version:               "{{ cilium_version | default('1.20.1') }}"
__kubectl_user:                 "{{ kubectl_user | default( ansible_env['SUDO_USER'] ) }}"
