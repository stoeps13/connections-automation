# stoeps.mongodb7

Builds the Connections MongoDB 7 image on a Kubernetes master and imports it into containerd on Kubernetes nodes.

The role is opt-in and is intended to be included by a play targeting `k8s_masters`.
